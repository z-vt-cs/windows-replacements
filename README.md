# Taskbar
- Replacing the windows taskbar

## Layout

- `frontend/`: Vite + React + TypeScript UI project.
- `frontend/src-tauri/`: Rust + Tauri desktop host.

## Getting Started

From `frontend/`:

- `npm install`
- `npm run tauri:dev`

## Notes

- Tauri on Windows uses the installed WebView2 runtime.